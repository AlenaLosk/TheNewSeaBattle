public class TheSeaBattle {
    public static void main(String[] args) {
        char[] cells = Field.createNewPlayField();
        for (int i = 1; i <= 4; i++) {
            Ship.createShips(cells, cells.length, i);
        }
        System.out.println(cells);
        Player.getShoot(cells);
    }
}
